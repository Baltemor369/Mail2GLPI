/**
 * Fichier de fonctions (FunctionFile) référencé par le manifeste.
 *
 * Le bouton de ruban ouvre directement le volet (action ShowTaskpane), donc aucune fonction
 * n'est nécessaire pour l'instant. Ce fichier est conservé comme point d'extension pour de
 * futures commandes (ex. « Créer le ticket » sans ouvrir le volet).
 */
Office.onReady(() => {
  // Aucune commande enregistrée pour le moment.
});
